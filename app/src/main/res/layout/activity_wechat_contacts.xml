<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/activity_wechat_contacts"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="com.swjtu.gcmformojo.WechatContactsActivity">

    <include layout="@layout/toolbar" />

<!--Button
    android:text="@string/btn_contacts_update"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="center"
    android:id="@+id/wechat_contacts_update" /-->

<ExpandableListView
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingTop="?attr/actionBarSize"
    android:id="@+id/wechat_friend_ExpandListView"
    android:groupIndicator="@null"/>

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/wechat_contacts_update"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|right"
        android:scaleType="center"
        android:layout_margin="16dp"
        app:srcCompat="@drawable/ic_refresh"
        app:fabSize="normal"/>
</android.support.design.widget.CoordinatorLayout>

